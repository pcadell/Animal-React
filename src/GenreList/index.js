import React from 'react'
import { Button } from 'semantic-ui-react'

const GenreList(props){

	const genres = props.genres.map((genre) => {
		return(
			<div>
				<h1 onClick={}>{genre}</a></h1>
			</div>
			)
	})


}